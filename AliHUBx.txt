-- by nome698 for PIML

local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- Criar GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TpBaseGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

-- Criar botão
local button = Instance.new("TextButton")
button.Name = "TpBase"
button.Size = UDim2.new(0, 120, 0, 40)
button.Position = UDim2.new(0, 10, 1, -50) -- canto inferior esquerdo
button.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
button.TextColor3 = Color3.new(1, 1, 1)
button.Text = "TpBase"
button.Font = Enum.Font.GothamBold
button.TextSize = 16
button.BorderSizePixel = 0
button.BackgroundTransparency = 0.2
button.Parent = screenGui

-- Teleporte direto
button.MouseButton1Click:Connect(function()
	local character = player.Character or player.CharacterAdded:Wait()
	local root = character:WaitForChild("HumanoidRootPart")

	local posAtual = root.Position
	local destino = Vector3.new(posAtual.X, 44.399, posAtual.Z)

	-- Desativa temporariamente o estado do humanoide (pra evitar cancelamento do teleport)
	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if humanoid then
		humanoid:ChangeState(Enum.HumanoidStateType.Physics)
	end

	-- TELEPORTA USANDO CFrame (rápido e direto)
	root.CFrame = CFrame.new(destino)

	-- Restaura o estado
	task.wait(0.1)
	if humanoid then
		humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
	end
end)
