-- by nome698 for PIML

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

-- Criar GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TpBaseGUI"
screenGui.Parent = player:WaitForChild("PlayerGui")
screenGui.ResetOnSpawn = false

-- Criar botão
local button = Instance.new("TextButton")
button.Name = "TpBase"
button.Size = UDim2.new(0, 120, 0, 40)
button.Position = UDim2.new(0, 10, 1, -50) -- canto inferior esquerdo
button.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
button.TextColor3 = Color3.new(1, 1, 1)
button.Text = "TpBase"
button.Font = Enum.Font.GothamBold
button.TextSize = 16
button.Parent = screenGui
button.BorderSizePixel = 0
button.BackgroundTransparency = 0.2
button.AutoButtonColor = true

local flying = false

local function teleportUp()
	if flying then return end
	local character = player.Character or player.CharacterAdded:Wait()
	local root = character:FindFirstChild("HumanoidRootPart")
	if not root then return end

	local targetY = 44.399
	if math.abs(root.Position.Y - targetY) < 1 then return end

	flying = true
	local connection

	connection = RunService.Heartbeat:Connect(function()
		if not root or not root.Parent then
			connection:Disconnect()
			flying = false
			return
		end

		local pos = root.Position
		if pos.Y >= targetY then
			root.AssemblyLinearVelocity = Vector3.zero
			connection:Disconnect()
			flying = false
			return
		end

		-- subir suavemente com velocidade
		root.AssemblyLinearVelocity = Vector3.new(0, 15, 0)
	end)
end

button.MouseButton1Click:Connect(teleportUp)
