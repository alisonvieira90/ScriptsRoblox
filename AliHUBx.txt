-- by nome698 for PIML

local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- Criar GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TpBaseGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

-- Criar botão
local button = Instance.new("TextButton")
button.Name = "TpBase"
button.Size = UDim2.new(0, 120, 0, 40)
button.Position = UDim2.new(0, 10, 1, -50) -- canto inferior esquerdo
button.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
button.TextColor3 = Color3.new(1, 1, 1)
button.Text = "TpBase"
button.Font = Enum.Font.GothamBold
button.TextSize = 16
button.BorderSizePixel = 0
button.BackgroundTransparency = 0.2
button.Parent = screenGui

-- Lógica de teleport
button.MouseButton1Click:Connect(function()
	local character = player.Character
	if not character then return end

	local root = character:FindFirstChild("HumanoidRootPart")
	if not root then return end

	local currentPos = root.Position
	local teleportPos = Vector3.new(currentPos.X, 44.399, currentPos.Z)

	-- Destrói o personagem para evitar anticheat
	character:BreakJoints()

	-- Quando ele renascer, seta a nova posição
	player.CharacterAdded:Wait():WaitForChild("HumanoidRootPart").CFrame = CFrame.new(teleportPos)
end)
